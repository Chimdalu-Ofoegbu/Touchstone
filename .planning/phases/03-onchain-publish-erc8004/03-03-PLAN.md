---
phase: 03-onchain-publish-erc8004
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - agent/src/mint-identity.ts
  - agent/src/identity-abi.ts
  - agent/src/registry-abi.ts
  - .planning/PROJECT.md
autonomous: false
requirements: [REQ-02, REQ-03]
user_setup:
  - service: pinata
    why: "Pin the agent-card JSON to IPFS to produce the agentURI for register()"
    env_vars:
      - name: PINATA_JWT
        source: "Pinata Dashboard -> API Keys (set in Plan 02; required before mint)"
must_haves:
  truths:
    - "The agent EOA holds an ERC-8004 Identity NFT minted from the canonical Mantle Mainnet registry"
    - "The minted agentId is captured and recorded (constructor arg + AGENT_TOKEN_ID env + PROJECT.md)"
    - "RatingRegistry is redeployed to Mantle Mainnet EXACTLY ONCE with registry+agentTokenId baked in"
    - "ownerOf(agentTokenId) == agent EOA on the live registry (gate will pass for the agent)"
    - "registry-abi.ts is reconciled against the post-redeploy artifact (ABI frozen for Phase 4)"
  artifacts:
    - path: "agent/src/mint-identity.ts"
      provides: "one-shot register(agentURI) mint capturing agentId"
      contains: "register"
    - path: ".planning/PROJECT.md"
      provides: "recorded agentId + Mainnet RatingRegistry address + agent-card CID"
      contains: "agentTokenId"
  key_links:
    - from: "agent/src/mint-identity.ts"
      to: "ERC-8004 Identity Registry 0x8004A169...432"
      via: "walletClient.writeContract register(agentURI)"
      pattern: "0x8004A169"
    - from: "script/Deploy.s.sol"
      to: "the minted agentId"
      via: "vm.envUint(AGENT_TOKEN_ID) constructor arg"
      pattern: "AGENT_TOKEN_ID"
---

<objective>
Run the FIRST ordering step of Phase 3: mint the agent's ERC-8004 Identity NFT from the canonical Mantle Mainnet registry via `register(agentURI)`, capture the returned `agentId`, then redeploy RatingRegistry to Mantle Mainnet EXACTLY ONCE with the registry address + agentTokenId baked in as immutables (the gate goes live; the ABI freezes). Reconcile registry-abi.ts against the post-redeploy artifact.

Purpose: REQ-03 (identity minted + held by agent) + REQ-02 (real contract on Mainnet). Everything downstream (Plan 04 publish, Plan 06 watcher) gates on this token existing and the contract being deployed. D-01: mint FIRST, redeploy ONCE, then iterate against a local Mainnet fork — never redeploy live again.
Output: A minted identity NFT held by the agent EOA, the recorded agentId, the once-only Mainnet RatingRegistry deploy, and the frozen ABI. This plan contains LIVE on-chain transactions (autonomous: false — human-confirmed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onchain-publish-erc8004/03-CONTEXT.md
@.planning/phases/03-onchain-publish-erc8004/03-RESEARCH.md
@.planning/phases/03-onchain-publish-erc8004/03-PATTERNS.md
@.planning/phases/03-onchain-publish-erc8004/03-01-SUMMARY.md
@.planning/phases/03-onchain-publish-erc8004/03-02-SUMMARY.md

<interfaces>
<!-- Reuse Plan 02's wallet + ipfs. Mint against the canonical registry. -->

From Plan 02 — agent/src/wallet.ts: `export const account; export const walletClient` (Mantle chain 5000, PRIVATE_KEY).
From Plan 02 — agent/src/ipfs.ts: `export async function pin(canonical: string): Promise<string>` (bare CID).
From agent/src/rpc.ts: `publicClient` (waitForTransactionReceipt), `redactRpcError`.

ERC-8004 Identity Registry (Mantle Mainnet ONLY): `0x8004A169FB4a3325136EB29fA0ceB6D2e539a432`
- ERC-721 (name="AgentIdentity", symbol="AGENT", supportsInterface(0x80ac58cd)=true)
- Mint surface (CONTEXT pre-flight, VERIFIED): `register(string agentURI) returns (uint256 agentId)` (selector 0xf2c298be); permissionless; mints to msg.sender.
- Emits `Registered(uint256 indexed agentId, string agentURI, address indexed owner)` AND ERC-721 `Transfer(0x0 -> agent, agentId)` (Assumption A1 — capture agentId from whichever is present; confirm at mint by reading the receipt logs).

Agent EOA (funded, VERIFIED 2026-06-10): `0xb27c7fa15D25E880Ba4a9a508e166538e106F51e` — holds 16.41 MNT on Mantle Mainnet (covers mint + redeploy + publish gas; deploys <$1).

EIP-8004 registration-v1 agent-card (the agentURI target — pin this small JSON first):
  required: type (literally "https://eips.ethereum.org/EIPS/eip-8004#registration-v1"), name, description, image, services (endpoints array), registrations (array — MAY be omitted at mint; agentId is only known after the tx — Open Question 3).

script/Deploy.s.sol (from Plan 01): reads `vm.envUint("AGENT_TOKEN_ID")` + uses the `IDENTITY_REGISTRY` constant. RPC alias `mantle = https://rpc.mantle.xyz` in foundry.toml; verification via Blockscout `--verifier blockscout --verifier-url https://explorer.mantle.xyz/api/` (no API key — DEC-deployment-target-plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint script + identity ABI + local-fork dry run (autonomous prep before the live tx)</name>
  <files>agent/src/mint-identity.ts, agent/src/identity-abi.ts</files>
  <read_first>
    - agent/src/wallet.ts + agent/src/ipfs.ts (from Plan 02 — walletClient + pin)
    - agent/src/rpc.ts (publicClient.waitForTransactionReceipt + redactRpcError)
    - agent/src/cli.ts (the main()/error-to-stderr one-shot idiom to mirror)
    - script/Deploy.s.sol (from Plan 01 — what AGENT_TOKEN_ID feeds)
    - 03-RESEARCH.md §5 (Pattern 5 — register(agentURI) mint + agent-card schema)
    - 03-PATTERNS.md → agent/src/mint-identity.ts section
  </read_first>
  <action>
    Build the one-shot mint as autonomous, dry-runnable code BEFORE the live tx. Implements REQ-03 (mint).

    Create `agent/src/identity-abi.ts`: export `identityRegistryAbi` (typed `as const`) covering `register(string) returns (uint256)`, the `Registered(uint256,string,address)` event, the ERC-721 `Transfer(address,address,uint256)` event, and `ownerOf(uint256) returns (address)`. Source: CONTEXT pre-flight + EIP-8004.

    Create `agent/src/mint-identity.ts` (mirror cli.ts main()/stderr idiom): (1) build the EIP-8004 registration-v1 agent-card JSON (type/name="Touchstone"/description/image/services; OMIT registrations at mint per Open Question 3); canonicalize it via the same `canonicalizeDoc`-style serialization or a stable JSON.stringify and `pin()` it → `cardCid`; (2) `agentURI = "ipfs://" + cardCid`; (3) `walletClient.writeContract({ address: IDENTITY, abi: identityRegistryAbi, functionName: "register", args: [agentURI], account, chain: mantle })`; wrap the catch in `redactRpcError`; (4) `waitForTransactionReceipt`, `parseEventLogs` for `Registered` (fallback to `Transfer` per Assumption A1) → log `agentId`, the tx hash, and the cardCid clearly to stdout. Add a `--dry-run` flag (or env guard) that runs steps 1-2 + simulates step 3 against `anvil --fork-url https://rpc.mantle.xyz` (or `publicClient.simulateContract`) WITHOUT broadcasting, so the script is proven before the single live mint. Const `IDENTITY = "0x8004A169FB4a3325136EB29fA0ceB6D2e539a432"` with a "Mantle Mainnet ONLY" comment.

    Dry-run verification (no live tx): start `anvil --fork-url https://rpc.mantle.xyz` and run the mint script in `--dry-run`/simulate mode against the fork — confirm the simulated `register` reaches the ERC-721 mint path and a `Registered`/`Transfer` log shape is parseable. This proves the code before the live step in Task 2.
  </action>
  <verify>
    <automated>cd agent && pnpm typecheck && grep -F '0x8004A169FB4a3325136EB29fA0ceB6D2e539a432' src/mint-identity.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && pnpm typecheck` exits 0
    - `grep -F 'functionName: "register"' agent/src/mint-identity.ts` matches
    - `grep -F 'ipfs://' agent/src/mint-identity.ts` matches (agentURI construction)
    - `grep -F 'Mantle Mainnet ONLY' agent/src/mint-identity.ts` matches (network discipline)
    - `grep -E 'Registered|Transfer' agent/src/identity-abi.ts` matches (agentId capture path)
    - `grep -F 'redactRpcError' agent/src/mint-identity.ts` matches (secret-scrub on the write path)
  </acceptance_criteria>
  <done>Mint script + identity ABI exist, type-check, dry-run-simulate against a Mainnet fork, parse agentId from the receipt, and scrub the RPC URL. No live tx yet.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: LIVE — mint the identity NFT, then redeploy RatingRegistry to Mantle Mainnet exactly once</name>
  <files>.env (AGENT_TOKEN_ID, RATING_REGISTRY_ADDRESS — human-set), on-chain state (Mantle Mainnet)</files>
  <action>HUMAN-ACTION CHECKPOINT (autonomous:false). Two LIVE on-chain transactions requiring human-confirmed broadcast plus a pending IPFS credential — no code is written in this task. Execute the steps in how-to-verify IN ORDER: mint FIRST (the agentId is the redeploy's constructor arg), then the once-only Mainnet redeploy (D-01: never iterate live), then record the addresses in .env + PROJECT.md.</action>
  <read_first>
    - 03-03 Task 1 output (the dry-run-proven mint script)
    - .planning/phases/01-lock-skeleton/01-03-DEPLOYMENT.md (the Phase 1 deploy+verify precedent + the Blockscout-503 forge verify-contract fallback)
    - script/Deploy.s.sol (the redeploy script reading AGENT_TOKEN_ID)
  </read_first>
  <what-built>
    Plan 01 produced the gated RatingRegistry + Deploy.s.sol (forge tests green). Task 1 produced the dry-run-proven mint script. This checkpoint executes the two LIVE on-chain transactions on Mantle Mainnet (chain 5000) from the funded agent EOA, in strict order (mint FIRST — the agentId is the redeploy's constructor arg). D-01: redeploy EXACTLY ONCE; never iterate live.
  </what-built>
  <how-to-verify>
    Pre-flight: confirm PINATA_JWT is set in root .env (Plan 02 prerequisite) and `cast balance 0xb27c7fa15D25E880Ba4a9a508e166538e106F51e --rpc-url https://rpc.mantle.xyz` shows sufficient MNT (~16 MNT funded).

    Step 1 — MINT (run once): `cd agent && pnpm mint-identity` (live). Observe the logged `agentId`, tx hash, and agent-card CID. Confirm on Mantlescan that `Transfer(0x0 -> 0xb27c7fa1..., agentId)` fired and `ownerOf(agentId)` returns the agent EOA (`cast call 0x8004A169FB4a3325136EB29fA0ceB6D2e539a432 "ownerOf(uint256)(address)" <agentId> --rpc-url https://rpc.mantle.xyz`). Set `AGENT_TOKEN_ID=<agentId>` in root .env.

    Step 2 — REDEPLOY (run once): `forge script script/Deploy.s.sol --rpc-url mantle --broadcast --verify --verifier blockscout --verifier-url https://explorer.mantle.xyz/api/` (reads AGENT_TOKEN_ID from env, bakes in the canonical registry constant). Record the deployed address; set `RATING_REGISTRY_ADDRESS=<addr>` in root .env. Confirm verification on Mantlescan and `cast call <addr> "agentTokenId()(uint256)" --rpc-url https://rpc.mantle.xyz` returns the minted agentId.

    Step 3 — record: append to .planning/PROJECT.md "Deployed Addresses" — the agentId, the agent EOA as NFT holder, the agent-card CID, and the new Mainnet RatingRegistry address + deploy tx + verification link.
  </how-to-verify>
  <verify>Manual evidence (live on-chain): `ownerOf(agentId)` returns the agent EOA; the deployed RatingRegistry's `agentTokenId()` returns the minted id; contract verified on Mantlescan. Captured in the resume signal + SUMMARY.</verify>
  <done>The agent EOA holds the minted ERC-8004 NFT; RatingRegistry is deployed once to Mainnet with agentTokenId baked in and verified; AGENT_TOKEN_ID + RATING_REGISTRY_ADDRESS set in .env; addresses recorded in PROJECT.md.</done>
  <resume-signal>Type "minted+deployed" with the agentId and the Mainnet RatingRegistry address, or describe any failure (e.g. mint reverted, verification 503 — fall back to forge verify-contract per the Phase 1 DEPLOYMENT.md precedent).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Reconcile registry-abi.ts against the post-redeploy artifact + record addresses</name>
  <files>agent/src/registry-abi.ts, .planning/PROJECT.md</files>
  <read_first>
    - agent/src/registry-abi.ts (from Plan 02 — the hand-authored skeleton to reconcile)
    - out/RatingRegistry.sol/RatingRegistry.json (the post-redeploy forge artifact — source of truth)
    - .planning/PROJECT.md "Deployed Addresses" section (the Phase 1 record format to mirror)
    - 03-RESEARCH.md Runtime State (ABI regen after the contract change; D-02 ABI freeze)
  </read_first>
  <action>
    Reconcile `agent/src/registry-abi.ts` against the authoritative `out/RatingRegistry.sol/RatingRegistry.json` produced by the Plan 01 contract (run `forge build` first to ensure the artifact is current). Replace the hand-authored ABI with the exact `abi` array from the artifact (or verify byte-for-byte equality of the relevant fragments) so viem inference matches the deployed bytecode precisely. This is the FROZEN ABI Phase 4 generates FE types from (D-02 ABI freeze) — note this in the module doc.

    Finalize `.planning/PROJECT.md`: under "Deployed Addresses", add a Phase 3 Mainnet row (RatingRegistry address, deploy tx, verification link, agentId, agent-card CID, agent EOA as NFT holder), and add a note that the gate is now the live `ownerOf(agentTokenId)` against `0x8004A169...432`. Confirm the recorded agentId matches `AGENT_TOKEN_ID` in .env and the contract's `agentTokenId()`.
  </action>
  <verify>
    <automated>cd agent && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && pnpm typecheck` exits 0 and `cd agent && pnpm test` exits 0 (ABI const valid, no regression)
    - `grep -F 'string' agent/src/registry-abi.ts` matches in the RatingPublished/publishRating fragment (cid present)
    - `grep -F 'agentTokenId' .planning/PROJECT.md` matches (agentId recorded)
    - `grep -F 'ownerOf' .planning/PROJECT.md` matches (gate-is-live note)
    - registry-abi.ts ABI equals the post-redeploy out/RatingRegistry.sol/RatingRegistry.json abi (reconciled, not hand-drifted)
  </acceptance_criteria>
  <done>registry-abi.ts matches the deployed contract artifact exactly (frozen for Phase 4); PROJECT.md records the agentId, agent-card CID, and Mainnet RatingRegistry address; the live gate is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent EOA → canonical registry | The mint tx; permissionless register mints the identity to msg.sender |
| agent EOA → Mantle Mainnet (deploy) | The once-only RatingRegistry deploy; deployer key = agent key |
| recorded agentId → contract constructor | The agentId baked as immutable must equal the minted token, or the gate is permanently mis-wired |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | wrong agentId baked into the immutable | mitigate | Task 2 verifies `ownerOf(agentId) == agent EOA` on the live registry BEFORE redeploy; Task 3 asserts `contract.agentTokenId() == AGENT_TOKEN_ID == recorded id`. A mismatch is caught before any publish. |
| T-03-11 | Tampering | gate pointed at wrong network | mitigate | Deploy.s.sol uses the `IDENTITY_REGISTRY` named constant (Mainnet-only comment, immutable); deploy targets `--rpc-url mantle` (chain 5000) only. |
| T-03-12 | Denial of Service | out-of-gas mid-mint/deploy | mitigate | Pre-flight `cast balance` check (~16 MNT funded covers mint + deploy + publish; deploys <$1). |
| T-03-13 | Information Disclosure | PRIVATE_KEY / PINATA_JWT in mint logs | mitigate | mint-identity.ts funnels catches through redactRpcError; never logs the key; PINATA_JWT only read from .env. |
| T-03-14 | Repudiation | redeploy more than once (D-01 violation) | mitigate | Deploy is a one-time checkpoint step; all subsequent iteration uses `anvil --fork` (Plan 04/06). PROJECT.md records the single canonical Mainnet address. |
</threat_model>

<verification>
- Live: `ownerOf(agentId)` on the canonical registry returns the agent EOA; `agentTokenId()` on the deployed RatingRegistry returns the same agentId; contract verified on Mantlescan.
- `cd agent && pnpm typecheck && pnpm test` — agent suite green with the reconciled ABI.
- PROJECT.md "Deployed Addresses" records the agentId, agent-card CID, and Mainnet RatingRegistry address.
</verification>

<success_criteria>
- The agent holds an ERC-8004 Identity NFT on Mantle Mainnet — maps to ROADMAP SC-1 (and the gate will pass for the agent / revert for others, proven by Plan 01's negative test + this live ownerOf).
- RatingRegistry is deployed once to Mainnet with the gate live and the ABI frozen — maps to ROADMAP SC-2/SC-3 (the real contract publish/read target).
</success_criteria>

<output>
After completion, create `.planning/phases/03-onchain-publish-erc8004/03-03-SUMMARY.md`
</output>
