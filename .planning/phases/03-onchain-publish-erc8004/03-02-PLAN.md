---
phase: 03-onchain-publish-erc8004
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/wallet.ts
  - agent/src/ipfs.ts
  - agent/src/registry-abi.ts
  - agent/package.json
  - .env.example
  - agent/tests/ipfs.test.ts
autonomous: true
requirements: [REQ-04]
user_setup:
  - service: pinata
    why: "Headless IPFS pinning of canonical reasoning JSON (the single LOCKED provider — DEC-ipfs-provider amended to Pinata 2026-06-10; raw-file CID, no directory wrap)"
    env_vars:
      - name: PINATA_JWT
        source: "Pinata Dashboard -> API Keys -> create key with pinning scope -> copy JWT"
      - name: PINATA_GATEWAY
        source: "Pinata Dashboard -> Gateways -> your dedicated gateway domain (optional; public gateway works)"
must_haves:
  truths:
    - "pin(canonical) uploads the EXACT canonicalizeDoc bytes (no JSON.stringify, no re-serialization) as a RAW FILE (not directory-wrapped) and returns a bare CID that resolves DIRECTLY to the JSON"
    - "walletClient signs from the agent PRIVATE_KEY on Mantle Mainnet (chain 5000)"
    - "the publish path scrubs the keyed RPC URL via redactRpcError"
    - "a standalone pin->fetch-by-CID-through-a-public-gateway->recompute-keccak256 round-trip proves the binding in ISOLATION (RUN_LIVE-gated) BEFORE Plan 04 wires pin into the publish pipeline"
  artifacts:
    - path: "agent/src/wallet.ts"
      provides: "viem walletClient + account from PRIVATE_KEY, Mantle chain 5000"
      contains: "privateKeyToAccount"
    - path: "agent/src/ipfs.ts"
      provides: "pin(canonical: string): Promise<string> returning a bare CID (Pinata raw-file path, no dir wrap)"
      contains: "export async function pin"
    - path: "agent/src/registry-abi.ts"
      provides: "RatingRegistry ABI const (as const) for viem write + parse + watch"
      contains: "ratingRegistryAbi"
    - path: "agent/tests/ipfs.test.ts"
      provides: "byte-exact pin round-trip test (mock provider asserts uploaded bytes === canonicalizeDoc) + RUN_LIVE pin->gateway->re-hash round-trip"
      contains: "canonicalizeDoc"
  key_links:
    - from: "agent/src/ipfs.ts"
      to: "the canonical hashed bytes"
      via: "new Blob([canonical]) — the exact string hash.ts hashed (Pitfall 1)"
      pattern: "new Blob\\(\\[canonical\\]"
    - from: "agent/src/wallet.ts"
      to: "agent/src/rpc.ts redactRpcError"
      via: "shared Mantle chain + RPC fallback + secret-scrub discipline"
      pattern: "privateKeyToAccount"
---

<objective>
Build the agent write-side infrastructure: a viem `walletClient` (the write twin of the existing read-only `publicClient`), a single-function `pin(canonical)` IPFS module that uploads the EXACT canonical bytes as a RAW FILE (no directory wrap) via Pinata and returns a bare CID, and a `registry-abi.ts` const for viem typing. Add a byte-exact pin round-trip test that proves the uploaded bytes equal `canonicalizeDoc(doc)`, plus a standalone RUN_LIVE-gated pin->fetch-by-CID-through-a-public-gateway->recompute-keccak256 round-trip that proves the integrity binding IN ISOLATION before Plan 04 consumes pin (Pitfall 1, T-3-determinism).

Purpose: These are the reusable primitives Plan 03 (mint) and Plan 04 (publish pipeline) compose. The byte-exactness + direct-CID-resolution of `pin()` is what makes the Phase 4 re-hash succeed — get it wrong and "verified on Mantle" never lights up. Proving the live round-trip in isolation here de-risks it BEFORE it is buried inside the publish pipeline.
Output: wallet.ts, ipfs.ts, registry-abi.ts (skeleton ABI, finalized post-redeploy in Plan 03), the pin byte-exact test + the isolated RUN_LIVE round-trip check, and the new env-var declarations. No network calls in the default suite (mock-path); the live round-trip is RUN_LIVE-gated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onchain-publish-erc8004/03-CONTEXT.md
@.planning/phases/03-onchain-publish-erc8004/03-RESEARCH.md
@.planning/phases/03-onchain-publish-erc8004/03-PATTERNS.md

<interfaces>
<!-- Reuse these directly. Extracted from the codebase — no exploration needed. -->

agent/src/rpc.ts (the read-side analog wallet.ts mirrors):
```typescript
const MANTLE_RPC_URL = process.env.MANTLE_RPC_URL ?? "https://rpc.mantle.xyz";
export const publicClient: PublicClient = createPublicClient({
  chain: mantle,
  transport: http(MANTLE_RPC_URL, { retryCount: 2, timeout: 15_000 }),
  batch: { multicall: true },
});
export function redactRpcUrl(message: string): string { /* scrubs MANTLE_RPC_URL */ }
export function redactRpcError(e: unknown): Error { /* wraps + scrubs */ }
```

agent/src/hash.ts (the single hash contract — reuse, do NOT re-implement):
```typescript
export function canonicalizeDoc(doc: ReasoningDocument): string; // RFC 8785 JCS
export function computeReasoningHash(doc: ReasoningDocument): Hex; // keccak256(toBytes(canonicalizeDoc(doc)))
```

pinata 2.5.6 (THE locked provider — raw-file CID, no directory wrap; DEC-ipfs-provider amended to Pinata 2026-06-10):
```typescript
import { PinataSDK } from "pinata";
const pinata = new PinataSDK({ pinataJwt: process.env.PINATA_JWT!, pinataGateway: process.env.PINATA_GATEWAY });
const { cid } = await pinata.upload.public.file(new Blob([canonical], { type: "application/json" })); // RAW-file CID — resolves DIRECTLY to the JSON, no dir wrap
```
The raw-file CID means `{gateway}/ipfs/{cid}` returns the exact JSON bytes (NOT a directory listing). This is the directory-wrap gotcha to avoid — do NOT use any directory-wrapping upload call. (RESEARCH §3 Pinata path + Pitfall 2 + Assumption A2.)

agent/package.json scripts (line 13 pattern to mirror): `"rate": "tsx --env-file-if-exists=../.env src/cli.ts"`. dependencies: viem ^2.52.2, canonicalize ^3.0.0, @anthropic-ai/sdk, zod.

Existing .env keys: PRIVATE_KEY, MANTLE_RPC_URL, MANTLE_SEPOLIA_RPC_URL, MANTLE_EXPLORER_KEY, ANTHROPIC_API_KEY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: walletClient + registry-abi skeleton + new env-var declarations</name>
  <files>agent/src/wallet.ts, agent/src/registry-abi.ts, .env.example, agent/package.json</files>
  <read_first>
    - agent/src/rpc.ts (the read-side analog — mirror chain/transport/fallback/redact)
    - agent/src/subjects/static.ts (lines 14, 38 — versioned named-const export style for registry-abi)
    - .env.example (current keys; add the new ones, NAMES ONLY)
    - agent/package.json (line 13 script pattern + dependencies block)
    - 03-RESEARCH.md §1 (Pattern 1 — walletClient) + Runtime State (new env keys)
    - 03-PATTERNS.md → agent/src/wallet.ts + agent/src/registry-abi.ts sections
  </read_first>
  <action>
    Create `agent/src/wallet.ts` mirroring rpc.ts: read `PRIVATE_KEY` and `MANTLE_RPC_URL` (same `?? "https://rpc.mantle.xyz"` fallback) from `process.env`; export `account = privateKeyToAccount(PRIVATE_KEY)` and `walletClient = createWalletClient({ account, chain: mantle, transport: http(MANTLE_RPC_URL, { retryCount: 2, timeout: 15_000 }) })`. Add the same module-doc-comment header style as rpc.ts noting the write path must funnel catches through `redactRpcError` (re-exported from rpc.ts — do NOT duplicate the redact logic).

    Create `agent/src/registry-abi.ts`: export `ratingRegistryAbi` as a typed `const ... as const` (for viem ABI inference), covering at minimum `publishRating(address,uint8,bytes32,uint8,string)`, the `RatingPublished` event (with the new `cid`), `RatingRequested`, `latestRating`, `ratingHistory`, and the `NotAgent`/`InvalidGrade`/`InvalidConfidence` errors. Add a module-doc note: "Generated from the POST-REDEPLOY artifact (out/RatingRegistry.sol/RatingRegistry.json). Plan 03 regenerates this after the once-only Mainnet redeploy; Phase 4 FE types derive from this same frozen ABI (D-02 ABI freeze)." Hand-author it now matching the Plan 01 contract shape so Plans 04/06 can type-check; Plan 03 reconciles it against the deployed artifact.

    NOTE (wave-1 parallel with Plan 01): this hand-authored ABI is written from the Plan 01 contract shape (the `cid` arg / `string` in `publishRating` + `RatingPublished`) while Plan 01's contract change is being made in parallel. A transient typecheck mismatch against an un-merged Plan 01 is EXPECTED and is reconciled byte-for-byte in Plan 03 Task 3 against `out/RatingRegistry.sol/RatingRegistry.json`. Author to the Plan 01 interface block shape; do not block on Plan 01.

    Update `.env.example`: append (NAMES ONLY, never values — T-2-01):
    ```
    # Phase 3 — minted ERC-8004 agent token id (set after running pnpm mint-identity, Plan 03)
    AGENT_TOKEN_ID=
    # Phase 3 — RatingRegistry address after the once-only Mainnet redeploy (Plan 03)
    RATING_REGISTRY_ADDRESS=
    # Phase 3 — IPFS pin (Pinata, the single LOCKED provider; raw-file CID). Required before publish/watch.
    PINATA_JWT=
    # Optional dedicated Pinata gateway (public gateway works if unset)
    PINATA_GATEWAY=
    ```
    Add a `"mint-identity"` and `"publish-rating"` and `"watch"` script stub to agent/package.json mirroring the `"rate"` line (`tsx --env-file-if-exists=../.env src/<file>.ts`) so Plans 03/04/06 only need to create the files. Add the pin dependency in a later install step (Task 2) — do not add it speculatively here.
  </action>
  <verify>
    <automated>cd agent && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && pnpm typecheck` exits 0
    - `grep -F 'privateKeyToAccount' agent/src/wallet.ts` matches
    - `grep -F 'chain: mantle' agent/src/wallet.ts` matches
    - `grep -F 'ratingRegistryAbi' agent/src/registry-abi.ts` matches and `grep -F 'string' agent/src/registry-abi.ts` matches (cid arg present)
    - `grep -E 'AGENT_TOKEN_ID|RATING_REGISTRY_ADDRESS|PINATA_JWT' .env.example` matches all three
    - `grep -c 'STORACHA' .env.example` returns 0 (single locked provider — no Storacha hedge)
    - `grep -F 'mint-identity' agent/package.json` matches (script stub wired)
  </acceptance_criteria>
  <done>walletClient signs from PRIVATE_KEY on Mantle chain 5000; registry-abi exports a typed const matching the Plan 01 contract; new env vars declared in .env.example (names only, Pinata-only — no Storacha); package.json scripts stubbed for Plans 03/04/06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pin(canonical) Pinata module + byte-exact mock test + isolated RUN_LIVE round-trip (the silent-failure guard)</name>
  <files>agent/src/ipfs.ts, agent/tests/ipfs.test.ts, agent/package.json</files>
  <read_first>
    - agent/src/hash.ts (canonicalizeDoc + computeReasoningHash — the bytes that MUST be pinned and re-hashed)
    - agent/src/rpc.ts (client-construction shape: module-level singleton from an env secret + one exported async fn)
    - 03-RESEARCH.md §3 (Pattern 3 — Pinata raw-file path) + Pitfall 1 + Pitfall 2 (directory-wrap gotcha) + Assumption A2 (fetch-the-CID-once round-trip)
    - 03-PATTERNS.md → agent/src/ipfs.ts section + Shared Patterns "Reasoning-hash determinism"
    - 03-VALIDATION.md (REQ-04 "Pinned bytes == hashed bytes" row)
  </read_first>
  <behavior>
    - pin(canonical) passes the EXACT input string to pinata.upload.public.file as a RAW FILE — the test asserts the bytes handed to the (mocked) upload call equal canonicalizeDoc(doc), NOT JSON.stringify(doc) (Pitfall 1)
    - pin returns a bare CID string (Pinata's returned raw-file cid, unwrapped — resolves directly to the JSON, no directory)
    - mock test injects a fake provider/upload fn (no network); asserts the Blob payload bytes round-trip byte-identical
    - RUN_LIVE round-trip (gated): real pin -> fetch {public gateway}/ipfs/{cid} -> the fetched bytes === canonical AND keccak256(fetched bytes) === computeReasoningHash(doc); proves direct-CID resolution (no dir listing) + the integrity binding in ISOLATION
  </behavior>
  <action>
    Install the locked pin provider: `cd agent && pnpm add pinata` (THE single provider per the amended DEC-ipfs-provider — raw-file CID; no Storacha). Implements REQ-04 + the D-02 silent-failure guard.

    Create `agent/src/ipfs.ts` exporting a single `export async function pin(canonical: string): Promise<string>`. Construct the PinataSDK at module level from `process.env.PINATA_JWT` (+ optional `process.env.PINATA_GATEWAY`), mirroring rpc.ts's env-secret-at-import pattern. Inside pin: `const blob = new Blob([canonical], { type: "application/json" });` then `const { cid } = await pinata.upload.public.file(blob);` and `return cid;` (bare RAW-file CID — D-02). CRITICAL constraints, stated in doc-comments:
      - (Pitfall 1) the input is the exact `canonicalizeDoc(doc)` string — NEVER `JSON.stringify(doc)` or a re-parsed object.
      - (Pitfall 2 / directory-wrap gotcha) use `upload.public.file` which returns a RAW-file CID that resolves DIRECTLY to the JSON — do NOT use any directory-wrapping upload path; the bare CID must `{gateway}/ipfs/{cid}` back to the exact bytes (NOT a directory listing).
    Funnel any thrown upload error through a redaction step so `PINATA_JWT` can never leak (do not log the key). Do NOT add a Storacha branch or a `PIN_PROVIDER` switch — Pinata is the single locked provider.

    Create `agent/tests/ipfs.test.ts` with TWO checks:
    1. DEFAULT (hermetic, no network): build a small valid `ReasoningDocument` (or import a Phase-2 fixture doc), compute `const canonical = canonicalizeDoc(doc)`. Inject a fake upload that captures the Blob it received, reads its bytes (`await blob.text()`), and asserts they `=== canonical` (byte-exact). Assert `pin()` returns the fake provider's CID unchanged. MUST NOT hit the network. This is the Pitfall-1 guard (T-3-determinism).
    2. RUN_LIVE-gated standalone round-trip (skipped unless `process.env.RUN_LIVE` is set — `it.skipIf(!process.env.RUN_LIVE)` or equivalent, so the default suite stays hermetic): with a real `PINATA_JWT`, call `pin(canonical)` for real, then `fetch(`https://${process.env.PINATA_GATEWAY ?? "gateway.pinata.cloud"}/ipfs/${cid}`)`, read the body bytes, and assert (a) the fetched bytes === canonical (proves RAW-file/direct-CID resolution — NOT a directory listing) and (b) `keccak256(toBytes(fetchedText)) === computeReasoningHash(doc)` (proves the on-chain hash binding). This is the isolated pin->fetch->re-hash round-trip proven BEFORE Plan 04 wires pin into the publish pipeline. Document `RUN_LIVE=1 pnpm test ipfs` in the SUMMARY.
  </action>
  <verify>
    <automated>cd agent && pnpm test ipfs && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && pnpm test ipfs` exits 0 (byte-exact mock round-trip green; live round-trip skipped without RUN_LIVE so the default suite stays hermetic)
    - `cd agent && pnpm typecheck` exits 0
    - `grep -F 'new Blob([canonical]' agent/src/ipfs.ts` matches (exact bytes, no JSON.stringify)
    - `grep -F 'upload.public.file' agent/src/ipfs.ts` matches (Pinata raw-file path — no directory wrap)
    - `grep -F 'export async function pin' agent/src/ipfs.ts` matches
    - `grep -c 'STORACHA\|@storacha\|wrapWithDirectory' agent/src/ipfs.ts` returns 0 (single locked provider; no directory-wrap path)
    - `grep -F 'canonicalizeDoc' agent/tests/ipfs.test.ts` matches (test asserts against the hashed bytes)
    - `grep -E 'RUN_LIVE' agent/tests/ipfs.test.ts` matches and `grep -F 'computeReasoningHash' agent/tests/ipfs.test.ts` matches (isolated pin->gateway->re-hash round-trip present)
    - `grep -F '/ipfs/' agent/tests/ipfs.test.ts` matches (gateway fetch-by-CID in the round-trip)
    - `grep -F '"pinata"' agent/package.json` matches (dependency added) and `grep -c '@storacha' agent/package.json` returns 0
    - `grep -vc JSON.stringify agent/src/ipfs.ts` — pin does NOT serialize the doc itself (the input is already canonical)
  </acceptance_criteria>
  <done>pin(canonical) uploads the exact canonical bytes as a RAW file (no dir wrap) and returns a bare CID that resolves directly to the JSON; the hermetic byte-exact test proves the uploaded payload equals canonicalizeDoc(doc); the RUN_LIVE round-trip proves pin->fetch-by-CID-through-a-gateway->recompute-keccak256 matches computeReasoningHash(doc) IN ISOLATION before Plan 04 consumes pin; pinata is the single installed provider (no Storacha).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → IPFS provider (Pinata) | Canonical reasoning bytes leave the agent; the returned raw-file CID is the integrity pointer |
| agent → Mantle RPC (write) | Signed transactions leave the agent; PRIVATE_KEY is the sole signing authority |
| env → process | Secrets (PRIVATE_KEY, PINATA_JWT) loaded from root .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | pinned bytes ≠ hashed bytes | mitigate | pin() uploads `new Blob([canonical])` — the exact `canonicalizeDoc` string — as a RAW file; the hermetic byte-exact test asserts it (Pitfall 1) and the RUN_LIVE round-trip re-fetches by CID and re-hashes to `computeReasoningHash(doc)` (Assumption A2) — proven in isolation before the pipeline. Single canonicalizer (hash.ts) for pin + hash. |
| T-03-06 | Information Disclosure | PINATA_JWT / PRIVATE_KEY in logs/errors | mitigate | Secrets read from root .env only (never committed — .env.example has names only); pin error path scrubs the JWT; write path funnels catches through redactRpcError (reused from rpc.ts). |
| T-03-07 | Information Disclosure | keyed RPC URL in walletClient transport errors | mitigate | walletClient reuses MANTLE_RPC_URL + the redactRpcError discipline; Plan 04 wraps the write call's catch in redactRpcError. |
| T-03-25 | Tampering | CID resolves to a directory listing, not the JSON (dir-wrap gotcha) | mitigate | pin() uses Pinata's `upload.public.file` raw-file path (no directory wrap); the RUN_LIVE round-trip asserts `{gateway}/ipfs/{cid}` returns the exact bytes (NOT a listing), so the Phase 4 FE fetch-by-bare-CID is guaranteed to work. |
</threat_model>

<verification>
- `cd agent && pnpm test ipfs` — byte-exact pin mock round-trip green (no network); live round-trip skipped without RUN_LIVE.
- `cd agent && RUN_LIVE=1 pnpm test ipfs` (manual, with PINATA_JWT) — pin->gateway-fetch-by-CID->re-hash matches computeReasoningHash(doc); proves direct-CID resolution + the integrity binding in isolation.
- `cd agent && pnpm typecheck` — wallet.ts + ipfs.ts + registry-abi.ts type-clean.
- `cd agent && pnpm test` — full default agent suite still green (no regression; live test excluded).
</verification>

<success_criteria>
- pin() is byte-exact AND resolves directly to the JSON by bare CID (the Phase 4 re-hash precondition), proven by the isolated RUN_LIVE round-trip — maps to ROADMAP SC-2 (reasoningHash == keccak256(canonical JSON)).
- walletClient is ready to sign publishRating from the agent key — maps to ROADMAP SC-2 (the agent writes publishRating).
- registry-abi exports the typed ABI Plans 04/06 consume (reconciled to the deployed artifact in Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/03-onchain-publish-erc8004/03-02-SUMMARY.md`
</output>
</content>
</invoke>
